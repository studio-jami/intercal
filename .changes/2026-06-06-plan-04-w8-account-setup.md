Plan 04 W8 adds the account and CLI setup runbook for Intercal operators.

- Documents provider prerequisites and proof commands for domain/DNS, SSH/VPS, Neon, R2, Upstash,
  Vertex/Gemini, GCloud Cloud Run/Cloud Build, GitHub Actions, Vercel, and local CLI auth.
- Adds a secret-safe handoff and rotation policy that links to the existing deployment, secrets,
  backups, and resource-budget runbooks instead of duplicating values or provider internals.
