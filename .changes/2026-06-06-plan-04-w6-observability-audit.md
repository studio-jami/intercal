---
type: fixed
area: operations
---

Corrected provider budget observability so missing provider telemetry returns unknown consumption
instead of zero-filled usage, and aligned the core snapshot helper with every SQL-owned view.
