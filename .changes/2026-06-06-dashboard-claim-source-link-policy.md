Workstream 5 citation closeout:

- Claim source-document metadata now uses the dashboard citation URL allowlist before rendering
  outbound links.
- Invalid, relative, or non-web source URLs remain available only as internal source-record state
  instead of public outbound citation links.
